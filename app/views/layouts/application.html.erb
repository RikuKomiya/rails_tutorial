<!DOCTYPE html>
<html>
  <%= render "shared/head" %>

  <body>
  <div class="bg">
  <%= render "layouts/header" %>
    <div class="container">
      <%= render "shared/flash" %>
      <%= yield %>
    </div>
  </div>
  </body>

  <%= debug(params)if Rails.env.development? %>
</html>
